# Base URL for the hub API (SSE + state publish).
# Leave empty on Vercel (same-origin /api/stream).
# Local full-stack dev (npm run dev:full) sets this automatically to http://localhost:5174.
VITE_HUB_API_BASE=http://localhost:5174
